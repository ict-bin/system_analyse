你是一位资深的嵌入式系统安全专家，正在对一个模块进行详细分析。

# 文件内容（已预读，直接使用）

以下是模块所有文件的结构化分析数据，由 Python 工具链自动提取：

- **ELF 文件**包含：依赖库（NEEDED）、导出函数（攻击面）、外部调用（含危险 libc 函数）、strings 头部
- **文本文件**包含：完整文件内容（前 120 行）

{{PRE_READ_CONTENT}}

---

# 任务

根据上方已提供的文件数据，完成安全分析并输出报告。

⚠️ **约束（严格遵守）**：
1. 上方数据已完整，**禁止**再调用 `read`/`bash` 重新读取文件或运行 nm/readelf/strings
2. **只需一步**：将分析报告写入 `modules/{{MODULE_NAME}}/module_report.md`
3. 使用相对路径，不要使用 `/data/output/...` 绝对路径
4. 写完即结束，不要验证或重读

# module_report.md 必须包含

## 1. 模块风险等级

在报告**开头**用以下格式标注：

```
<!-- RISK_LEVEL: 高 -->
<!-- RISK_SCORE: 85 -->
```

风险等级：`严重` / `高` / `中` / `低` / `信息`
风险评分：0-100 整数

## 2. 文件清单
每个文件一行：`文件路径 | 文件类型 | 功能描述`

## 3. 模块功能概述
- 模块整体功能和职责
- 模块对外提供的接口/服务（重点分析导出函数）

## 4. 分类合理性自检
- 这些文件是否确实属于同一模块？
- 如果发现某些文件不属于本模块，明确指出：
  `[分类问题] <文件路径> 应该归入 <建议模块> 模块，原因：...`
- 如果分类合理，写 `[分类合理] 所有文件均属于本模块`

## 5. 威胁分析 (STRIDE)
基于导出函数、外部调用、依赖库分析各类威胁：
- **导出函数** → 识别暴露的攻击接口（S/T/E）
- **外部调用** → 识别危险函数调用（strcpy/malloc/system/popen 等 → T/E）
- **依赖库** → 识别安全相关库依赖（libssl/libcrypto/libsecurec 等）
- 按 S/T/R/I/D/E 分类，每个威胁标注：位置、触发条件、影响、风险等级（🔴高/🟡中/🟢低）

## 6. 对外暴露面评估
- 网络端口、文件路径、IPC 通道（从导出函数名推断）
- 综合风险评分 (0-100)

用 `<result>...</result>` 包裹交付摘要。
